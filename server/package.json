{
  "scripts": {
    "start": "npm run build",
    "build": "tsc -p app.ts"
  },
  "dependencies": {
    "pngquant-bin": "^5.0.2",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.7"
  }
}
